import React from 'react';
import Carousel from '../Carousel/Carousel';
import Items from '../Items/Items';

const Home = () => {
    return (
        <div>
            <Carousel></Carousel>
            <Items></Items>
        </div>
    );
};

export default Home;